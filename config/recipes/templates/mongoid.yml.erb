<%= rails_env %>:
  sessions:
    default:
      database: <%= fetch(:application) %>_<%= rails_env %>
      username: <%= fetch(:user) %>
      password: <%= mongodb_password %>
      pool_size: 15

      hosts:
        - localhost:27017
      options:
        skip_version_check: true
        safe: true

